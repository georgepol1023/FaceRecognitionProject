# Face Recognition Project

This project uses Python, dlib, and OpenCV to perform real-time face recognition via webcam.

## Setup

1. Install Python 3.12
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
